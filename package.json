{
  "name": "zotero-acp",
  "version": "0.1.0",
  "description": "Zotero plugin implementing Agent Client Protocol (ACP)",
  "main": "build/bootstrap.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "lint": "eslint src/**/*.ts"
  },
  "author": "Rafael",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3",
    "@types/firefox-webext-browser": "^120.0.1"
  }
}